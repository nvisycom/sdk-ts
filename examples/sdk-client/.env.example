# Your Nvisy API token. Copy this file to .env and fill it in.
NVISY_API_TOKEN=

# Optional: override the API base URL (defaults to https://api.nvisy.com).
# NVISY_BASE_URL=http://127.0.0.1:8080

# Optional: an uploaded file id to trigger a pipeline run.
# NVISY_FILE_ID=
