# The webhook's signing secret, returned once from webhooks.createWebhook()
# as WebhookCreated.secret. Copy this file to .env and fill it in.
NVISY_WEBHOOK_SECRET=whsec_...

# Optional: port to listen on (default 3000)
# PORT=3000
